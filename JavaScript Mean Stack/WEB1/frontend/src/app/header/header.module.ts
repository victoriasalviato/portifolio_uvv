import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

// O HeaderComponent não precisa ser importado ou declarado aqui
@NgModule({
  imports: [CommonModule], // Importa apenas CommonModule, se necessário
})
export class HeaderModule { }
